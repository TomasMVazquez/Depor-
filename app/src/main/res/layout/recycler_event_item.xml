<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="event"
            type="com.applications.toms.depormas.domain.Event" />
        <variable
            name="clickListener"
            type="com.applications.toms.depormas.ui.adapters.EventListener" />
    </data>

    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/primaryColor"
        app:cardCornerRadius="17dp"
        app:cardPreventCornerOverlap="true"
        android:layout_marginTop="8dp"
        android:elevation="8dp"
        app:strokeWidth="1dp"
        app:strokeColor="@color/semi_transparent"
        android:clickable="true"
        android:focusable="true"
        android:checkable="true"
        app:rippleColor="@color/primaryDarkColor"
        android:onClick="@{()->clickListener.onClick(event)}"
        >

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="2dp"
            >

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <ImageView
                    android:id="@+id/dashboardImg"
                    android:layout_width="25dp"
                    android:layout_height="25dp"
                    android:layout_marginStart="8dp"
                    android:padding="2dp"
                    android:contentDescription="@string/content_description_sport_item"
                    sportImg="@{event.sport}"
                    app:tint="@color/secondaryDarkColor"
                    tools:srcCompat="@drawable/ic_running" />

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@{event.getDateTime()}"
                    android:gravity="center"
                    android:textAppearance="@style/TextAppearance.AppCompat.Medium"
                    android:textColor="@color/secondaryDarkColor"
                    tools:text="25/05/2021 - 15:00hs" />
            </LinearLayout>

            <View
                android:id="@+id/divider3"
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginBottom="1dp"
                android:background="@android:color/darker_gray"
                />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:weightSum="100">

                <com.google.android.gms.maps.MapView
                    android:id="@+id/eventMap"
                    android:layout_width="0dp"
                    android:layout_weight="30"
                    android:layout_height="match_parent"/>

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_weight="70"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="8dp"
                    >

                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:text="@{event.location.name}"
                        android:textAppearance="@style/TextAppearance.AppCompat.Small"
                        android:textColor="@color/secondaryColor"
                        android:textStyle="bold"
                        tools:text="Calle Eduardo Minguito 15" />

                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:text="@{event.location.city}"
                        android:textAppearance="@style/TextAppearance.AppCompat.Small"
                        android:textColor="@color/secondaryColor"
                        android:textStyle="bold"
                        tools:text="Madrid" />

                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:text="@{@string/item_km_from_you(0)}"
                        tools:text="a 4.3 KM de vos"
                        android:textAppearance="@style/TextAppearance.AppCompat.Small"
                        android:textColor="@color/secondaryLightColor" />

                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:text="@{@string/item_participant(event.participants)}"
                        tools:text="Participan: 1"
                        android:textAppearance="@style/TextAppearance.AppCompat.Small"
                        android:textColor="@color/secondaryLightColor" />

                </LinearLayout>
            </LinearLayout>
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>
</layout>